#====================================================
# Funcao para gerar parametros dos experimentos
createExperiment = function(array_size) {
  x = sprintf("%s00000", array_size)
  return(x)
}
#====================================================